venv/
frontend/
notebooks/
tests/
outputs/
__pycache__/
*.pyc
.env
.git/
.claude/
# NOTE: data/external/ is intentionally NOT ignored — the API needs
# data/external/fjelstul/matches.csv at runtime to build Elo history for the
# /api/regression endpoint (and the bracket sim). Only raw dumps are excluded.
data/raw/
*.ipynb
docs/
app/
